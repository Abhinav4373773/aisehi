services:
  - type: web
    name: notebook-runner
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python run_notebook.py
    repo: https://github.com/yourusername/your-repo
    branch: main
    autoDeploy: true
